/* eslint-disable */
// import { setSessionInfo } from '@/until/session'
const subMenuList = {
  state: {
    subMenuList: []
  },
  mutations: {
    setMenuList (state, val) {
      state.subMenuList = val
      // setSessionInfo("myValue", val)
    }
  },
  action: {

  }
}

export default subMenuList
